{
  "name": "archdata-repos",
  "private": true,
  "scripts": {
    "setup": "lerna bootstrap",
    "release": "lerna publish",
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  }
}
