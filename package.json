{
  "name": "archdata-repos",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "setup": "lerna bootstrap",
    "release": "lerna publish --force-publish",
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  }
}
